root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/server ."
bin = "./tmp/server"
full_bin = "PORT=${PORT:-8081} SQLITE_PATH=${SQLITE_PATH:-data/easy-login.db} ./tmp/server"
include_ext = ["go"]
exclude_dir = ["tmp", "data"]
delay = 200
stop_on_error = true

[log]
time = true
